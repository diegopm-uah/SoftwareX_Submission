
def value_arg(exception, filePath):
    """
        check_file_arg:: Exception -> Path -> IO Path | IO Exception
        check whether the arg is an angle given in degres or radians
        exception provides a suitable exception to raise, from the caller
    """
    # if not (filePath.exists() and filePath.is_file()):                          # check path is readable file
    #     raise exception                                                         # or raise exception
    # return filePath  
    pass